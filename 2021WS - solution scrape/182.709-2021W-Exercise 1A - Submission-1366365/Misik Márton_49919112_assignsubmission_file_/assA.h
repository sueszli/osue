#ifndef ASSA_H
#define ASSA_H

/**
 * 
 * @student: Márton Misik
 * @matrikelnummer: 11915287
 *
 * @description: assA.c contains the main function. The main function handles all input with argument handling by
 *               getopt, then forwards this line-by-line to the evaluator, which then returns the verdict, whether the line is a
 *               palindrom or not. Then the results get appended and printed either to the console or a file.
 * 
 * @first_created: 11/11/2021
 * 
 */

#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>
#include "utils.h"
#include <getopt.h>
#include <stdbool.h>
#include <string.h>

#endif //ASSA_H