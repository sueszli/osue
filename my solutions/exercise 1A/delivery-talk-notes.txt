Exercise 1A:
  - Forgot error handling at file output
    ```
      if (fclose(outputStream) == EOF) {
        error("fclose failed");
      }
    ```

Feedback: should open `stdin` as a stream and be able to read multiple lines from the input