/**
 * @author Maximilian Maresch
 */ 

/**
 * @brief Converts a string to an integer
 * @detail This function is used to convert the string str to an integer.
 * @param str - the string to convert
 * @return an integer representing str
 */ 
int hexStrToInt(char *str);

/**
 * @brief Frees multiple strings
 * @detail This function is used to free multiple strings.
 * @param str1 - a string to free
 * @param str2 - a string to free
 */ 
void freeAll2(char* str1, char* str2);

/**
 * @brief Frees multiple strings
 * @detail This function is used to free multiple strings.
 * @param str1 - a string to free
 * @param str2 - a string to free
 * @param str3 - a string to free
 */ 
void freeAll3(char* str1, char* str2, char* str3);

/**
 * @brief Frees multiple strings
 * @detail This function is used to free multiple strings.
 * @param str1 - a string to free
 * @param str2 - a string to free
 * @param str3 - a string to free
 * @param str4 - a string to free
 */ 
void freeAll4(char* str1, char* str2, char* str3, char* str4);